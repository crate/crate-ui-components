// this file is used to supply
// example translations for the tests. Without valid translations
// the tests will pass but will dump debug into the terminal.
// Here we setup dedicated test translations that will be merged
// with the main translations in the test setup. This way we we
// don't need to arbitrarily import messages from the application.
const testMessages = {
  'test.submitButtonLabel': 'click',
};

export default testMessages;
